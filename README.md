# HumanDetection
This repository is an experiment in which I trained an AI model to detect human faces based on pre-existing data for training. I then deployed it's usage onto a frontend through fastAPI in order to wrap my backend to connect with my frontend's requests.
